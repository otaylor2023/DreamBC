[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dreambc-cosmos-flash"
version = "0.1.0"
description = "DreamBC cosmos_transfer Flash worker"
requires-python = ">=3.11"
dependencies = [
    "runpod-flash",
    "google-cloud-storage",
]
